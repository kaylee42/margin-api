This Rails API is partner to the [Margin](github.com/eaud/Margin) app built in Ember. 

Cloning this repo and running `bundle install` will install the necessary dependencies to get it up and running. 
